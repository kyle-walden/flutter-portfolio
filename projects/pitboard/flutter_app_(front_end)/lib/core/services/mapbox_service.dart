// Mapbox placeholder integration

class MapboxService {
  // Intentionally no API keys here. For real usage, platform-specific setup and keys are required.
  static const String style = 'mapbox://styles/mapbox/streets-v11';

  static void init() {
    // Placeholder: set access token via native config or runtime (not stored in repo)
  }
}
